{
  "mcpServers": {
    "shared-memory": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@sfrangulov/shared-memory-mcp"],
      "env": {
        "GITHUB_TOKEN": "${GITHUB_TOKEN}",
        "GITHUB_REPO": "${GITHUB_REPO}"
      }
    }
  }
}
